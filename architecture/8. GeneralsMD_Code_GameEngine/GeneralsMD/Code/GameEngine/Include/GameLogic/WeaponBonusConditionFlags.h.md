# GeneralsMD/Code/GameEngine/Include/GameLogic/WeaponBonusConditionFlags.h

## Purpose
Defines a type alias for weapon bonus condition flags used in the game logic system.

## Responsibilities
- Declares the `WeaponBonusConditionFlags` type alias.
- Provides a header guard to prevent multiple inclusions.
- Part of header detangling effort (JKMCD Aug 2002).

## Key Types
- WeaponBonusConditionFlags (typedef): Type alias for `UnsignedInt` representing weapon bonus condition flags.

## Key Functions
None.

## Globals
None.

## Dependencies
- `UnsignedInt` (external type).
- Standard header guards (`#pragma once`, `#ifndef`/`#define`/`#endif`).
