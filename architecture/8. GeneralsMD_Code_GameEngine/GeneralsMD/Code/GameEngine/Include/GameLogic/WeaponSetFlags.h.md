# GeneralsMD/Code/GameEngine/Include/GameLogic/WeaponSetFlags.h

## Purpose
Defines a bitflag type for weapon set flags used in the game logic system.

## Responsibilities
- Declares the `WeaponSetFlags` typedef for bitflag operations on weapon sets
- Includes necessary header for `WEAPONSET_COUNT` definition
- Provides header guard to prevent multiple inclusions

## Key Types
- WeaponSetFlags (typedef): Bitflag container for weapon set types

## Key Functions
None

## Globals
None

## Dependencies
- `GameLogic/WeaponSetType.h` (for `WEAPONSET_COUNT`)
- `BitFlags` template (assumed from engine codebase)
